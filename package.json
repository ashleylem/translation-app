{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "create-css": "npx tailwindcss -i ./static/style.css -o static/css/main.css --watch",
    "pytesseract-ocr": "sudo apt-get update && sudo apt-get install -y tesseract-ocr tesseract-ocr-spa"
  },
  "devDependencies": {
    "tailwindcss": "^3.3.2"
  }
}
